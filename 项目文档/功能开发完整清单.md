# AI跨境新手加速器（Auto选品）- 完整功能开发清单

> 文档版本：v1.0  
> 更新时间：2026-04-13  
> 产品阶段：MVP完成 → 完整版开发  
> 总预计工时：约 48-52 人周

---

## 目录

1. [用户系统](#一-用户系统)
2. [AI选品系统](#二-ai选品系统)
3. [数据分析系统](#三-数据分析系统)
4. [内容生成系统](#四-内容生成系统)
5. [课程系统](#五-课程系统)
6. [运营系统](#六-运营系统)
7. [开发排期总览](#七-开发排期总览)

---

## 一、用户系统

### 1.1 基础用户模块

| 属性 | 内容 |
|------|------|
| **功能名称** | 用户注册与登录 |
| **功能描述** | 支持邮箱、手机号、第三方社交账号（微信、Google、Apple）注册登录，支持验证码登录和密码登录 |
| **技术实现要点** | - JWT无状态认证，支持Refresh Token自动续期<br>- 短信/邮件验证码服务集成（阿里云/腾讯云SMS）<br>- OAuth2.0第三方登录集成<br>- 密码强度校验+bcrypt加密存储<br>- 登录失败锁定机制（5次/15分钟） |
| **接口设计** | `POST /api/auth/register` 注册<br>`POST /api/auth/login` 登录<br>`POST /api/auth/logout` 登出<br>`POST /api/auth/refresh-token` 刷新Token<br>`POST /api/auth/forgot-password` 忘记密码<br>`POST /api/auth/reset-password` 重置密码<br>`GET /api/auth/oauth/:provider` 第三方登录跳转<br>`GET /api/auth/oauth/callback/:provider` 第三方登录回调 |
| **数据库设计** | `users` 表：id, email, phone, password_hash, nickname, avatar_url, status, email_verified, phone_verified, created_at, updated_at<br>`oauth_accounts` 表：id, user_id, provider, provider_user_id, access_token, created_at<br>`login_attempts` 表：id, user_id, ip, attempt_type, success, created_at |
| **前端页面** | 登录页 `/login`、注册页 `/register`、忘记密码页 `/forgot-password`、第三方登录弹窗 |
| **测试用例** | TC-001: 邮箱注册成功<br>TC-002: 手机号+验证码注册成功<br>TC-003: 微信OAuth登录成功<br>TC-004: 登录失败5次后账户锁定<br>TC-005: Token过期自动刷新<br>TC-006: 忘记密码-邮件验证码发送与重置 |
| **预计工时** | 5 人天 |
| **优先级** | P0 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 会员体系 |
| **功能描述** | 三级会员体系：免费版（体验）、专业版（月付/年付）、企业版（定制），不同版本享有不同功能权限和API调用配额 |
| **技术实现要点** | - 会员等级存储在users表+Redis缓存<br>- 订阅支付集成Stripe/支付宝/微信支付<br>- Webhook接收支付回调更新会员状态<br>- 到期前7天/3天/1天自动提醒<br>- 会员权益基于规则引擎（Casbin）动态控制 |
| **接口设计** | `GET /api/membership/plans` 获取会员套餐列表<br>`POST /api/membership/subscribe` 订阅/升级<br>`GET /api/membership/current` 获取当前会员信息<br>`POST /api/membership/cancel` 取消订阅<br>`GET /api/membership/invoice/:id` 获取发票<br>`POST /api/webhooks/payment` 支付回调 |
| **数据库设计** | `membership_plans` 表：id, name, code, price_monthly, price_yearly, features_json, quota_daily, quota_monthly, sort_order, status<br>`user_memberships` 表：id, user_id, plan_id, status, started_at, expires_at, auto_renew, created_at<br>`subscription_transactions` 表：id, user_id, plan_id, amount, currency, payment_method, transaction_id, status, created_at |
| **前端页面** | 会员中心 `/membership`、套餐对比页 `/pricing`、支付页 `/checkout`、订阅管理 `/subscription` |
| **测试用例** | TC-007: 免费用户访问付费功能被拦截<br>TC-008: 月付转年付差价计算正确<br>TC-009: 会员到期自动降级<br>TC-010: 支付回调更新会员状态 |
| **预计工时** | 6 人天 |
| **优先级** | P0 |

---

### 1.2 权限与安全

| 属性 | 内容 |
|------|------|
| **功能名称** | 角色权限管理（RBAC） |
| **功能描述** | 支持管理员、商户运营、客服等不同角色，每个角色拥有不同功能权限和数据访问范围 |
| **技术实现要点** | - RBAC权限模型：用户-角色-权限三层关系<br>- 权限数据存储在MySQL+Redis缓存<br>- 路由守卫+接口鉴权双重保障<br>- 敏感操作需二次验证（OTP）<br>- 操作日志完整记录（谁、何时、何操作） |
| **接口设计** | `GET /api/roles` 获取角色列表<br>`POST /api/roles` 创建角色<br>`PUT /api/roles/:id` 更新角色<br>`DELETE /api/roles/:id` 删除角色<br>`GET /api/permissions` 获取权限列表<br>`POST /api/users/:id/roles` 分配角色<br>`GET /api/users/:id/permissions` 获取用户权限<br>`GET /api/operation-logs` 操作日志查询 |
| **数据库设计** | `roles` 表：id, name, code, description, created_at, updated_at<br>`permissions` 表：id, name, code, module, action, description<br>`role_permissions` 表：role_id, permission_id<br>`user_roles` 表：user_id, role_id<br>`operation_logs` 表：id, user_id, action, target_type, target_id, before_data, after_data, ip, user_agent, created_at |
| **前端页面** | 角色管理 `/admin/roles`、权限配置 `/admin/roles/:id/permissions`、用户角色分配 `/admin/users/:id/roles`、操作日志 `/admin/logs` |
| **测试用例** | TC-011: 普通用户无法访问管理后台<br>TC-012: 角色权限变更即时生效<br>TC-013: 删除角色后用户权限正确回收 |
| **预计工时** | 4 人天 |
| **优先级** | P1 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 多设备登录管理 |
| **功能描述** | 支持用户查看登录设备列表、远程强制下线、单设备优先登录、登录设备异常提醒 |
| **技术实现要点** | - 设备指纹识别（User-Agent + IP + 设备特征）<br>- JWT绑定设备ID实现单设备控制<br>- WebSocket实时推送异常登录通知<br>- Redis存储在线设备会话 |
| **接口设计** | `GET /api/devices` 获取登录设备列表<br>`DELETE /api/devices/:id` 移除设备<br>`DELETE /api/devices` 移除所有其他设备<br>`POST /api/devices/:id/set-primary` 设置主设备<br>`POST /api/devices/:id/trust` 信任设备 |
| **数据库设计** | `user_devices` 表：id, user_id, device_id, device_name, device_type, ip_address, user_agent, last_active_at, is_trusted, is_primary, created_at |
| **前端页面** | 设备管理 `/settings/devices`、安全中心 `/settings/security` |
| **测试用例** | TC-014: 强制下线后旧Token立即失效<br>TC-015: 新设备登录推送通知 |
| **预计工时** | 2 人天 |
| **优先级** | P2 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 实名认证与KYC |
| **功能描述** | 支持个人实名认证和企业认证，提升信任度和部分高级功能解锁 |
| **技术实现要点** | - 阿里云/腾讯云实人认证API集成<br>- 营业执照OCR识别+自动填表<br>- 认证状态多版本管理（待审核/通过/拒绝）<br>- 认证信息加密存储 |
| **接口设计** | `POST /api/kyc/personal/submit` 提交个人认证<br>`POST /api/kyc/enterprise/submit` 提交企业认证<br>`GET /api/kyc/status` 查询认证状态<br>`GET /api/kyc/detail` 获取认证详情 |
| **数据库设计** | `kyc_records` 表：id, user_id, type, status, id_card_number(加密), id_card_front_url, id_card_back_url, business_license_url, company_name, company_address, verified_at, rejected_reason, created_at |
| **前端页面** | 实名认证页 `/settings/kyc`、认证结果页 `/settings/kyc/result` |
| **测试用例** | TC-016: 身份证OCR识别正确填表<br>TC-017: 企业认证审核流程完整 |
| **预计工时** | 3 人天 |
| **优先级** | P2 |

---

## 二、AI选品系统

### 2.1 选品分析核心

| 属性 | 内容 |
|------|------|
| **功能名称** | 智能选品分析 |
| **功能描述** | 核心功能：基于AI算法分析市场需求、竞争度、利润空间，输出选品建议报告，支持关键词/类目/竞品多维度分析 |
| **技术实现要点** | - 多数据源接入：Amazon、eBay、TikTok Shop、速卖通等平台API<br>- AI模型：LLM(GPT-4/Claude)+结构化数据分析<br>- 评分算法：市场需求(30%) + 竞争度(25%) + 利润空间(25%) + 趋势(20%)<br>- 异步任务队列（Celery/ Bull）处理大规模分析<br>- 结果缓存Redis + 定时更新机制 |
| **接口设计** | `POST /api/products/analyze` 创建选品分析任务<br>`GET /api/products/analyze/:taskId` 查询分析进度<br>`GET /api/products/analyze/:taskId/result` 获取分析结果<br>`POST /api/products/analyze/batch` 批量分析<br>`GET /api/products/recommendations` 获取精选推荐<br>`POST /api/products/compare` 竞品对比分析 |
| **数据库设计** | `analysis_tasks` 表：id, user_id, task_type, params_json, status, progress, result_url, error_message, started_at, completed_at, created_at<br>`analysis_results` 表：id, task_id, product_name, category, platform, demand_score, competition_score, profit_score, trend_score, overall_score, price_range, monthly_demand, competitor_count, supplier_cost, recommendation, created_at<br>`product_cache` 表：id, platform, product_id, product_data_json, updated_at |
| **前端页面** | 选品分析首页 `/products/analyze`、分析报告详情 `/products/analyze/:id`、精选推荐 `/products/recommendations`、竞品对比 `/products/compare` |
| **测试用例** | TC-018: 单一产品分析返回完整报告<br>TC-019: 批量分析10个产品成功<br>TC-020: 分析进度实时更新<br>TC-021: 缓存结果命中加速 |
| **预计工时** | 10 人天 |
| **优先级** | P0 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 爆款追踪 |
| **功能描述** | 监控指定商品/类目的销量变化、价格变动、排名波动，第一时间发现爆款趋势 |
| **技术实现要点** | - 定时爬虫/官方API获取数据（每小时/每日）<br>- 同比/环比增长率计算<br>- 异常波动告警规则引擎<br>- 历史数据存储与趋势可视化<br>- 支持按商品ASIN/店铺/类目监控 |
| **接口设计** | `POST /api/tracking/products` 添加追踪商品<br>`GET /api/tracking/products` 获取追踪列表<br>`DELETE /api/tracking/products/:id` 删除追踪<br>`GET /api/tracking/products/:id/history` 获取历史数据<br>`GET /api/tracking/trends` 获取趋势榜单<br>`POST /api/tracking/alerts` 设置告警规则<br>`GET /api/tracking/alerts` 获取告警列表 |
| **数据库设计** | `tracked_products` 表：id, user_id, platform, product_id, product_name, image_url, current_price, current_rank, alert_enabled, created_at<br>`product_daily_snapshots` 表：id, tracked_product_id, date, price, rank, sales_estimate, reviews_count, rating, created_at<br>`alert_rules` 表：id, user_id, product_id, alert_type, threshold, enabled, created_at |
| **前端页面** | 爆款追踪首页 `/tracking`、商品详情页 `/tracking/:id`、告警设置 `/tracking/alerts`、趋势榜单 `/trends` |
| **测试用例** | TC-022: 添加ASIN追踪成功<br>TC-023: 价格波动触发告警通知<br>TC-024: 历史趋势图表正确渲染 |
| **预计工时** | 6 人天 |
| **优先级** | P0 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 趋势预测 |
| **功能描述** | 基于历史数据+AI算法预测品类未来趋势，识别蓝海市场和新兴需求 |
| **技术实现要点** | - 时序预测模型（Prophet/LSTM）<br>- Google Trends数据分析<br>- 社交媒体热度分析（TikTok、Instagram）<br>-季节性/事件性因素叠加<br>- 预测置信区间可视化 |
| **接口设计** | `POST /api/forecast/category` 预测类目趋势<br>`POST /api/forecast/keyword` 预测关键词趋势<br>`GET /api/forecast/result/:id` 获取预测结果<br>`GET /api/forecast/opportunities` 获取蓝海机会<br>`GET /api/forecast/seasonality` 获取季节性分析 |
| **数据库设计** | `forecast_tasks` 表：id, user_id, forecast_type, target, params_json, status, created_at, completed_at<br>`forecast_results` 表：id, task_id, date, predicted_value, confidence_low, confidence_high, factors_json, created_at<br>`opportunity_keywords` 表：id, keyword, category, opportunity_score, demand_trend, competition_level, recommended_at |
| **前端页面** | 趋势预测页 `/forecast`、蓝海机会页 `/forecast/opportunities`、预测详情 `/forecast/:id` |
| **测试用例** | TC-025: 关键词趋势预测成功<br>TC-026: 蓝海机会列表生成<br>TC-027: 季节性分析图表准确 |
| **预计工时** | 8 人天 |
| **优先级** | P1 |

---

### 2.2 数据采集与来源

| 属性 | 内容 |
|------|------|
| **功能名称** | 多平台数据源对接 |
| **功能描述** | 集成Amazon、eBay、TikTok Shop、速卖通、Wish等主流跨境平台数据，统一清洗后供分析使用 |
| **技术实现要点** | - 各平台官方API接入（亚马逊SP-API、eBay API等）<br>- 第三方数据平台集成（Helium10、Jungle Scout模拟）<br>- 数据清洗标准化管道<br>- 增量更新+全量同步双模式<br>- 数据源健康度监控 |
| **接口设计** | `GET /api/datasources` 获取数据源列表<br>`POST /api/datasources/connect` 连接数据源<br>`POST /api/datasources/sync` 手动触发同步<br>`GET /api/datasources/:id/status` 数据源状态<br>`DELETE /api/datasources/:id` 断开连接 |
| **数据库设计** | `data_sources` 表：id, name, platform, api_type, credentials_json, status, last_sync_at, sync_status, created_at<br>`platform_products` 表：id, platform, product_id, asin, title, category, brand, price, rating, reviews_count, sales_rank, images_json, created_at, updated_at |
| **前端页面** | 数据源管理 `/settings/datasources`、数据同步日志 `/settings/datasources/logs` |
| **测试用例** | TC-028: Amazon SP-API OAuth连接成功<br>TC-029: 数据同步任务正常执行<br>TC-030: 数据源异常告警触发 |
| **预计工时** | 8 人天 |
| **优先级** | P1 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 供应链数据整合 |
| **功能描述** | 整合1688、阿里国际站、环球资源等供应商数据，辅助选品后的货源寻找 |
| **技术实现要点** | - 1688开放平台API集成<br>- 1688商品搜索与详情抓取<br>- 价格区间分析（起订量梯度）<br>- 供应商信誉评分<br>- 物流成本估算（头程+尾程） |
| **接口设计** | `POST /api/suppliers/search` 搜索供应商<br>`GET /api/suppliers/:id` 供应商详情<br>`POST /api/suppliers/compare` 供应商对比<br>`GET /api/suppliers/product/:id` 关联产品找供应商<br>`POST /api/suppliers/calculate-cost` 成本计算 |
| **数据库设计** | `suppliers` 表：id, platform, supplier_id, name, location, rating, response_rate, product_count, created_at<br>`supplier_products` 表：id, supplier_id, product_id, price, moq, freight_estimate, created_at<br>`cost_calculations` 表：id, user_id, product_id, supplier_id, quantity, cost_breakdown_json, profit_estimate, created_at |
| **前端页面** | 供应商搜索 `/suppliers`、供应商详情 `/suppliers/:id`、成本计算器 `/suppliers/calculator` |
| **测试用例** | TC-031: 1688商品搜索返回结果<br>TC-032: 成本计算器利润估算准确 |
| **预计工时** | 5 人天 |
| **优先级** | P1 |

---

## 三、数据分析系统

### 3.1 看板与报表

| 属性 | 内容 |
|------|------|
| **功能名称** | 数据看板 |
| **功能描述** | 可自定义的仪表盘，展示核心业务指标（GMV、订单量、转化率、用户增长等），支持拖拽布局和多图表组合 |
| **技术实现要点** | - ECharts图表库封装组件<br>- 看板布局存储（用户自定义）<br>- 实时数据WebSocket推送<br>- 图表缓存加速渲染<br>- 导出为PDF/PNG |
| **接口设计** | `GET /api/dashboard/config` 获取看板配置<br>`POST /api/dashboard/config` 保存看板配置<br>`GET /api/dashboard/widgets/:id/data` 获取图表数据<br>`POST /api/dashboard/export` 导出看板 |
| **数据库设计** | `dashboard_configs` 表：id, user_id, name, layout_json, is_default, created_at, updated_at<br>`dashboard_widgets` 表：id, config_id, widget_type, data_source, params_json, position_json, created_at |
| **前端页面** | 我的看板 `/dashboard`、看板编辑 `/dashboard/edit`、图表组件库 `/dashboard/widgets` |
| **测试用例** | TC-033: 拖拽添加图表组件<br>TC-034: 看板配置保存成功<br>TC-035: 图表数据实时更新 |
| **预计工时** | 5 人天 |
| **优先级** | P1 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 销售报表 |
| **功能描述** | 生成销售数据报表，支持按日/周/月/年维度，自动生成同比环比分析，支持订阅邮件定时推送 |
| **技术实现要点** | - 数据聚合查询优化（物化视图/预计算）<br>- Excel/PDF报表生成（Apache POI/ wkhtmltopdf）<br>- 邮件定时任务集成<br>- 多店铺数据汇总 |
| **接口设计** | `POST /api/reports/sales` 生成销售报表<br>`GET /api/reports/:id` 获取报表详情<br>`GET /api/reports` 报表列表<br>`POST /api/reports/:id/export` 导出报表<br>`POST /api/reports/subscribe` 订阅报表 |
| **数据库设计** | `reports` 表：id, user_id, type, name, params_json, file_url, status, generated_at, created_at |
| **前端页面** | 报表中心 `/reports`、报表详情 `/reports/:id`、报表订阅 `/reports/subscribe` |
| **测试用例** | TC-036: 月度报表生成成功<br>TC-037: PDF导出格式正确<br>TC-038: 定时邮件推送正常 |
| **预计工时** | 4 人天 |
| **优先级** | P1 |

---

### 3.2 智能预警

| 属性 | 内容 |
|------|------|
| **功能名称** | 智能预警系统 |
| **功能描述** | 基于规则和AI的异常检测，自动发现销售下滑、库存异常、差评暴增等问题，及时推送预警 |
| **技术实现要点** | - 规则引擎（配置化预警规则）<br>- 统计异常检测（Z-Score/移动平均）<br>- 多渠道推送（站内信/邮件/短信/钉钉/企微）<br>- 预警收敛（避免同一问题重复告警）<br>- 预警处理工作流（处理/忽略/升级） |
| **接口设计** | `GET /api/alerts` 获取预警列表<br>`GET /api/alerts/:id` 预警详情<br>`POST /api/alerts/:id/handle` 处理预警<br>`POST /api/alerts/rules` 创建预警规则<br>`GET /api/alerts/rules` 预警规则列表<br>`PUT /api/alerts/rules/:id` 更新规则<br>`DELETE /api/alerts/rules/:id` 删除规则 |
| **数据库设计** | `alert_rules` 表：id, user_id, name, type, condition_json, threshold, severity, channels_json, enabled, created_at<br>`alerts` 表：id, user_id, rule_id, title, content, severity, status, source_data_json, handled_at, handled_by, created_at |
| **前端页面** | 预警中心 `/alerts`、预警规则 `/alerts/rules`、预警设置 `/alerts/settings` |
| **测试用例** | TC-039: 销售额下跌20%触发预警<br>TC-040: 预警通知成功发送邮件<br>TC-041: 预警收敛避免重复通知 |
| **预计工时** | 5 人天 |
| **优先级** | P1 |

---

## 四、内容生成系统

### 4.1 Listing生成

| 属性 | 内容 |
|------|------|
| **功能名称** | AI Listing生成 |
| **功能描述** | 基于产品信息和选品分析结果，一键生成符合平台规则的Listing（标题、五点描述、长描述、A+内容） |
| **技术实现要点** | - LLM提示词工程优化（平台化/本地化）<br>- 关键词埋入（SEO优化）<br>- 多平台适配模板（Amazon/eBay/TikTok等）<br>- 违规词过滤<br>- 生成内容版权检测 |
| **接口设计** | `POST /api/listing/generate` 生成Listing<br>`POST /api/listing/optimize` 优化现有Listing<br>`POST /api/listing/localize` 翻译适配<br>`GET /api/listing/templates` 获取模板列表<br>`POST /api/listing/templates` 创建模板 |
| **数据库设计** | `listing_templates` 表：id, user_id, platform, name, template_content, variables_json, created_at, updated_at<br>`generated_listings` 表：id, user_id, product_id, platform, title, bullet_points, description, keywords_json, seo_score, created_at |
| **前端页面** | Listing生成页 `/listing/create`、Listing编辑 `/listing/:id`、模板管理 `/listing/templates` |
| **测试用例** | TC-042: 英文Listing生成质量合格<br>TC-043: 关键词SEO评分>80<br>TC-044: 多平台适配成功 |
| **预计工时** | 5 人天 |
| **优先级** | P0 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 图片处理 |
| **功能描述** | AI辅助图片处理：主图优化（白底/背景去除）、图片增强、智能裁剪、图片生成（文生图） |
| **技术实现要点** | - 图像识别与分割（Remove.bg API / Segment Anything）<br>- Stable Diffusion图片生成<br>- 图片压缩优化（WebP格式）<br>- CDN加速分发 |
| **接口设计** | `POST /api/images/remove-bg` 背景去除<br>`POST /api/images/enhance` 图片增强<br>`POST /api/images/generate` AI生成图片<br>`POST /api/images/resize` 智能裁剪<br>`POST /api/images/compress` 图片压缩 |
| **数据库设计** | `processed_images` 表：id, user_id, original_url, processed_url, operation_type, params_json, created_at |
| **前端页面** | 图片处理工具 `/tools/images`、图片库 `/images` |
| **测试用例** | TC-045: 白底图生成成功<br>TC-046: 文生图符合描述<br>TC-047: 图片压缩率达标 |
| **预计工时** | 4 人天 |
| **优先级** | P1 |

---

### 4.2 翻译与本地化

| 属性 | 内容 |
|------|------|
| **功能名称** | 多语言翻译 |
| **功能描述** | 支持20+语种的AI翻译，专有词汇库确保翻译准确性，支持批量翻译和翻译记忆 |
| **技术实现要点** | - 多翻译引擎集成（DeepL/Google/有道）<br>- 翻译记忆库（TM）<br>- 术语库管理<br>- 翻译质量评分<br>- 人工校对工作流 |
| **接口设计** | `POST /api/translate` 单条翻译<br>`POST /api/translate/batch` 批量翻译<br>`GET /api/translate/languages` 支持语言列表<br>`GET /api/translate/memories` 翻译记忆库<br>`POST /api/translate/glossary` 添加术语 |
| **数据库设计** | `translation_memories` 表：id, user_id, source_lang, target_lang, source_text, target_text, created_at<br>`translation_glossary` 表：id, user_id, term, translations_json, created_at |
| **前端页面** | 翻译工具 `/translate`、术语库 `/translate/glossary` |
| **测试用例** | TC-048: 中译英准确性>90%<br>TC-049: 术语库生效覆盖通用翻译 |
| **预计工时** | 3 人天 |
| **优先级** | P1 |

---

### 4.3 视频脚本

| 属性 | 内容 |
|------|------|
| **功能名称** | 视频脚本生成 |
| **功能描述** | AI生成TikTok/短视频带货脚本，支持多种风格（种草、测评、开箱、剧情），附带分镜建议和BGM推荐 |
| **技术实现要点** | - 爆款脚本模板库<br>- 平台热门话题Tag集成<br>- 时长/节奏控制<br>- 口播台词+画面描述分离<br>- AIGC视频生成（可选） |
| **接口设计** | `POST /api/scripts/generate` 生成视频脚本<br>`GET /api/scripts/:id` 获取脚本详情<br>`GET /api/scripts/templates` 脚本模板列表<br>`POST /api/scripts/templates` 创建模板 |
| **数据库设计** | `script_templates` 表：id, user_id, name, style, duration, platform, template_content, created_at<br>`generated_scripts` 表：id, user_id, product_id, template_id, title, content_json, estimated_duration, created_at |
| **前端页面** | 脚本生成 `/scripts/create`、脚本库 `/scripts`、模板市场 `/scripts/templates` |
| **测试用例** | TC-050: 种草风格脚本生成成功<br>TC-051: 脚本包含完整分镜<br>TC-052: 预估时长准确 |
| **预计工时** | 4 人天 |
| **优先级** | P2 |

---

## 五、课程系统

### 5.1 学习体系

| 属性 | 内容 |
|------|------|
| **功能名称** | 课程中心 |
| **功能描述** | 跨境电商全链路课程体系，从入门到进阶，涵盖选品、运营、物流、客服等全流程 |
| **技术实现要点** | - 课程分类（类目树形结构）<br>- 视频点播（阿里云/腾讯云VOD）<br>- 课程难度分级（入门/进阶/高阶）<br>- 讲师主页与课程包<br>- 课程试看功能 |
| **接口设计** | `GET /api/courses` 课程列表<br>`GET /api/courses/:id` 课程详情<br>`GET /api/courses/:id/chapters` 章节列表<br>`POST /api/courses/:id/enroll` 报名课程<br>`GET /api/courses/:id/progress` 学习进度 |
| **数据库设计** | `courses` 表：id, title, description, cover_url, difficulty, category_id, instructor_id, price, duration, lessons_count, enrolled_count, rating, status, created_at<br>`course_chapters` 表：id, course_id, title, sort_order<br>`course_lessons` 表：id, chapter_id, title, video_url, duration, sort_order, is_preview |
| **前端页面** | 课程首页 `/courses`、课程详情 `/courses/:id`、课程播放页 `/courses/:id/learn/:lessonId` |
| **测试用例** | TC-053: 课程列表筛选功能正常<br>TC-054: 视频播放流畅<br>TC-055: 课程报名成功 |
| **预计工时** | 6 人天 |
| **优先级** | P1 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 学习进度管理 |
| **功能描述** | 自动追踪学习进度，支持断点续学，学习数据统计，学时证明生成 |
| **技术实现要点** | - 视频播放节点上报（每30秒上报）<br>- 进度实时存储Redis+异步持久化MySQL<br>- 跨设备同步学习进度<br>- 完成率计算（视频进度≥95%为完成）<br>- PDF学时证明生成 |
| **接口设计** | `POST /api/progress/sync` 同步学习进度<br>`GET /api/progress/course/:id` 获取课程进度<br>`GET /api/progress/all` 获取全部学习进度<br>`GET /api/progress/certificate` 获取学时证明 |
| **数据库设计** | `learning_progress` 表：id, user_id, course_id, lesson_id, progress_percent, last_position, completed_at, updated_at |
| **前端页面** | 我的学习 `/my/learning`、学习详情 `/my/learning/:id`、学时证明 `/my/certificates` |
| **测试用例** | TC-056: 视频播放中途退出后恢复<br>TC-057: 课程完成状态正确更新<br>TC-058: 学时证明PDF生成正确 |
| **预计工时** | 3 人天 |
| **优先级** | P1 |

---

### 5.2 学习激励

| 属性 | 内容 |
|------|------|
| **功能名称** | 学习激励体系 |
| **功能描述** | 学习积分、等级称号、排行榜、成就徽章等游戏化激励机制，提升用户学习积极性 |
| **技术实现要点** | - 积分规则引擎（看视频/完成测验/分享等）<br>- 等级经验值计算<br>- 成就系统配置化<br>- 排行榜定时更新（Redis Sorted Set）<br>- 徽章解锁推送 |
| **接口设计** | `GET /api/achievements` 成就列表<br>`POST /api/achievements/:id/claim` 领取成就奖励<br>`GET /api/leaderboard/:type` 排行榜<br>`GET /api/user/points` 我的积分<br>`GET /api/user/level` 我的等级 |
| **数据库设计** | `achievements` 表：id, code, name, description, icon_url, points_reward, condition_json, created_at<br>`user_achievements` 表：id, user_id, achievement_id, claimed_at<br>`user_points` 表：id, user_id, points, level, experience, updated_at<br>`points_logs` 表：id, user_id, action, points, balance, description, created_at |
| **前端页面** | 成就中心 `/achievements`、排行榜 `/leaderboard`、我的等级 `/my/level` |
| **测试用例** | TC-059: 完成课程获得积分<br>TC-060: 成就解锁推送成功<br>TC-061: 排行榜数据准确 |
| **预计工时** | 4 人天 |
| **优先级** | P2 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 课程证书 |
| **功能描述** | 完成课程后生成电子证书，支持下载分享，证书可验证真伪 |
| **技术实现要点** | - 证书模板设计（Canvas API）<br>- 区块链存证（可选，提升可信度）<br>- 证书唯一编号生成<br>- 证书验证接口<br>- 社交分享（生成分享图片） |
| **接口设计** | `POST /api/certificates/generate` 生成证书<br>`GET /api/certificates/:id` 证书详情<br>`GET /api/certificates/:id/verify` 验证证书<br>`GET /api/certificates/:id/download` 下载证书 |
| **数据库设计** | `certificates` 表：id, user_id, course_id, certificate_no, issued_at, file_url, blockchain_hash, verified_count, created_at |
| **前端页面** | 我的证书 `/my/certificates`、证书展示 `/certificates/:id` |
| **测试用例** | TC-062: 课程完成后生成证书<br>TC-063: 证书编号唯一可验证<br>TC-064: PDF下载格式正确 |
| **预计工时** | 2 人天 |
| **优先级** | P2 |

---

## 六、运营系统

### 6.1 通知与消息

| 属性 | 内容 |
|------|------|
| **功能名称** | 站内消息系统 |
| **功能描述** | 系统通知、用户消息、订单提醒等站内消息，支持消息分类、未读红点、消息提醒设置 |
| **技术实现要点** | - 消息类型：系统通知、业务提醒、用户消息<br>- 消息存储与查询优化（分页+索引）<br>- 未读数实时统计（Redis）<br>- 消息订阅/退订<br>- 消息过期自动清理（90天） |
| **接口设计** | `GET /api/notifications` 消息列表<br>`POST /api/notifications/read` 标记已读<br>`POST /api/notifications/read-all` 全部已读<br>`GET /api/notifications/unread-count` 未读数<br>`PUT /api/notifications/settings` 通知设置 |
| **数据库设计** | `notifications` 表：id, user_id, type, title, content, data_json, is_read, read_at, created_at<br>`notification_settings` 表：id, user_id, type, enabled, created_at |
| **前端页面** | 消息中心 `/notifications`、通知设置 `/settings/notifications` |
| **测试用例** | TC-065: 消息发送成功<br>TC-066: 未读数实时更新<br>TC-067: 消息分类筛选正确 |
| **预计工时** | 2 人天 |
| **优先级** | P1 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 消息推送 |
| **功能描述** | 支持邮件、短信、App推送（Push）、微信服务号等多渠道消息推送，支持定时推送和用户分群推送 |
| **技术实现要点** | - 消息渠道：邮件(SendGrid)、短信(阿里云)、极光/个推<br>- 消息模板管理<br>- 用户分群（标签/行为）<br>- 定时任务调度（XXL-JOB）<br>- 推送效果追踪（送达/打开/点击） |
| **接口设计** | `POST /api/push/send` 发送推送<br>`POST /api/push/batch` 批量推送<br>`POST /api/push/schedule` 定时推送<br>`GET /api/push/templates` 模板列表<br>`POST /api/push/templates` 创建模板<br>`GET /api/push/stats/:id` 推送统计 |
| **数据库设计** | `push_templates` 表：id, name, channel, subject, content, variables_json, created_at<br>`push_records` 表：id, template_id, user_ids_json, status, scheduled_at, sent_at, stats_json, created_at<br>`push_stats` 表：id, record_id, sent, delivered, opened, clicked, created_at |
| **前端页面** | 消息推送 `/push`、模板管理 `/push/templates`、推送统计 `/push/stats` |
| **测试用例** | TC-068: 邮件推送送达成功<br>TC-069: 定时推送准时发送<br>TC-070: 分群推送准确触达 |
| **预计工时** | 5 人天 |
| **优先级** | P1 |

---

### 6.2 活动运营

| 属性 | 内容 |
|------|------|
| **功能名称** | 活动中心 |
| **功能描述** | 管理运营活动：新手礼包、限时优惠、邀请返利、节日活动等，支持活动配置、奖品管理、中奖概率控制 |
| **技术实现要点** | - 活动规则引擎（时间/条件/概率）<br>- 奖品库存管理<br>- 防薅羊毛机制（IP/设备/账号限制）<br>- 活动数据实时统计<br>- 活动自动上下线 |
| **接口设计** | `GET /api/activities` 活动列表<br>`GET /api/activities/:id` 活动详情<br>`POST /api/activities/:id/participate` 参与活动<br>`GET /api/activities/:id/prizes` 我的奖品<br>`POST /api/activities/:id/claim` 领取奖品 |
| **数据库设计** | `activities` 表：id, name, type, description, start_at, end_at, rules_json, status, created_at<br>`activity_prizes` 表：id, activity_id, name, type, value, stock, probability, created_at<br>`activity_participations` 表：id, activity_id, user_id, prize_id, status, participated_at |
| **前端页面** | 活动中心 `/activities`、活动详情 `/activities/:id`、我的奖品 `/activities/prizes` |
| **测试用例** | TC-071: 活动参与逻辑正确<br>TC-072: 奖品库存扣减准确<br>TC-073: 防薅机制生效 |
| **预计工时** | 5 人天 |
| **优先级** | P2 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 邀请裂变 |
| **功能描述** | 用户邀请新用户注册，双方获得奖励，支持邀请排行榜、邀请阶梯奖励 |
| **技术实现要点** | - 邀请关系链存储<br>- 唯一邀请码生成<br>- 阶梯奖励计算<br>- 邀请统计实时更新<br>- 提现审批流程 |
| **接口设计** | `GET /api/invite/code` 获取邀请码<br>`POST /api/invite/share` 生成分享链接<br>`GET /api/invite/stats` 邀请统计<br>`GET /api/invite/records` 邀请记录<br>`POST /api/invite/withdraw` 提现申请 |
| **数据库设计** | `invite_codes` 表：id, user_id, code, used_count, created_at<br>`invite_records` 表：id, inviter_id, invitee_id, reward_amount, status, rewarded_at, created_at<br>`withdraw_requests` 表：id, user_id, amount, status, processed_at, created_at |
| **前端页面** | 邀请中心 `/invite`、邀请排行 `/invite/leaderboard`、我的奖励 `/invite/rewards` |
| **测试用例** | TC-074: 邀请码绑定关系正确<br>TC-075: 阶梯奖励计算准确<br>TC-076: 提现申请审批通过 |
| **预计工时** | 4 人天 |
| **优先级** | P2 |

---

### 6.3 客服支持

| 属性 | 内容 |
|------|------|
| **功能名称** | 在线客服 |
| **功能描述** | IM即时通讯客服系统，支持文字/图片/文件沟通，智能客服机器人+人工客服协作 |
| **技术实现要点** | - WebSocket实时消息<br>- 消息存储与漫游<br>- 智能客服（LLM+RAG知识库）<br>- 客服排队与分配<br>- 满意度评价 |
| **接口设计** | `GET /api/chat/conversations` 会话列表<br>`POST /api/chat/conversations` 创建会话<br>`GET /api/chat/messages/:id` 消息历史<br>`POST /api/chat/messages/:id` 发送消息<br>`POST /api/chat/rate/:id` 满意度评价 |
| **数据库设计** | `conversations` 表：id, user_id, agent_id, status, rating, created_at, updated_at<br>`messages` 表：id, conversation_id, sender_type, sender_id, content, attachments_json, created_at |
| **前端页面** | 客服中心 `/support`、会话页面 `/support/:id`、客服工作台 `/agent` |
| **测试用例** | TC-077: 消息即时送达<br>TC-078: 机器人回复准确<br>TC-079: 满意度评价成功 |
| **预计工时** | 8 人天 |
| **优先级** | P2 |

---

| 属性 | 内容 |
|------|------|
| **功能名称** | 帮助中心与知识库 |
| **功能描述** | 自助帮助文档、FAQ、视频教程，支持智能搜索和问题推荐 |
| **技术实现要点** | - Markdown文档渲染<br>- 全文搜索引擎（Meilisearch/Elasticsearch）<br>- 智能推荐（RAG增强搜索）<br>- 文档分类与标签<br>- 用户反馈优化搜索 |
| **接口设计** | `GET /api/help/articles` 文章列表<br>`GET /api/help/articles/:id` 文章详情<br>`GET /api/help/search` 搜索<br>`GET /api/help/faqs` FAQ列表<br>`POST /api/help/feedback` 搜索反馈 |
| **数据库设计** | `help_articles` 表：id, category_id, title, content, tags_json, views, helpful_count, created_at, updated_at<br>`help_categories` 表：id, name, parent_id, sort_order<br>`help_feedback` 表：id, article_id, user_id, helpful, created_at |
| **前端页面** | 帮助中心 `/help`、文章详情 `/help/:id`、搜索结果 `/help/search` |
| **测试用例** | TC-080: 文章搜索相关性强<br>TC-081: 文档渲染格式正确 |
| **预计工时** | 3 人天 |
| **优先级** | P2 |

---

## 七、开发排期总览

### 7.1 第一周：P0核心功能 ⭐⭐⭐

| 序号 | 模块 | 功能名称 | 预计工时 | 负责人 | 依赖 |
|------|------|----------|----------|--------|------|
| 1 | 用户系统 | 用户注册与登录 | 5d | 后端-小王 | 无 |
| 2 | 用户系统 | 会员体系 | 6d | 后端-小李 | 1 |
| 3 | AI选品 | 智能选品分析 | 10d | 后端-架构师 | 外部API |
| 4 | AI选品 | 爆款追踪 | 6d | 后端-小张 | 3 |
| 5 | 内容生成 | AI Listing生成 | 5d | 后端-小刘 | 3 |
| **小计** | | | **32d** | | |

### 7.2 第二周：P1重要功能 ⭐⭐

| 序号 | 模块 | 功能名称 | 预计工时 | 负责人 | 依赖 |
|------|------|----------|----------|--------|------|
| 6 | 用户系统 | 角色权限管理（RBAC） | 4d | 后端-小王 | 1 |
| 7 | AI选品 | 趋势预测 | 8d | 后端-架构师 | 3 |
| 8 | AI选品 | 多平台数据源对接 | 8d | 后端-小李 | 3 |
| 9 | AI选品 | 供应链数据整合 | 5d | 后端-小张 | 8 |
| 10 | 数据分析 | 数据看板 | 5d | 前端-小赵 | 后端API |
| 11 | 数据分析 | 销售报表 | 4d | 后端-小刘 | 8 |
| 12 | 数据分析 | 智能预警系统 | 5d | 后端-小陈 | 8 |
| 13 | 内容生成 | 图片处理 | 4d | 后端-小张 | AI服务 |
| 14 | 内容生成 | 多语言翻译 | 3d | 后端-小刘 | AI服务 |
| 15 | 运营系统 | 站内消息系统 | 2d | 后端-小王 | 1 |
| 16 | 运营系统 | 消息推送 | 5d | 后端-小李 | 外部服务 |
| 17 | 课程系统 | 课程中心 | 6d | 前后端-小赵/小陈 | 视频服务 |
| 18 | 课程系统 | 学习进度管理 | 3d | 后端-小陈 | 17 |
| **小计** | | | **62d** | | |

### 7.3 第三周：P2完善功能 ⭐

| 序号 | 模块 | 功能名称 | 预计工时 | 负责人 | 依赖 |
|------|------|----------|----------|--------|------|
| 19 | 用户系统 | 多设备登录管理 | 2d | 后端-小王 | 1 |
| 20 | 用户系统 | 实名认证与KYC | 3d | 后端-小李 | 1 |
| 21 | 内容生成 | 视频脚本生成 | 4d | 后端-小刘 | AI服务 |
| 22 | 课程系统 | 学习激励体系 | 4d | 后端-小陈 | 17 |
| 23 | 课程系统 | 课程证书 | 2d | 后端-小陈 | 17 |
| 24 | 运营系统 | 活动中心 | 5d | 后端-小王 | 1 |
| 25 | 运营系统 | 邀请裂变 | 4d | 后端-小李 | 1 |
| 26 | 运营系统 | 在线客服 | 8d | 前后端-小赵/小陈 | 1 |
| 27 | 运营系统 | 帮助中心与知识库 | 3d | 后端-小刘 | 全文搜索 |
| **小计** | | | **35d** | | |

### 7.4 第四周：优化迭代 🔧

| 内容 | 说明 | 预计工时 |
|------|------|----------|
| 性能优化 | 数据库索引优化、Redis缓存优化、CDN配置 | 5d |
| 安全加固 | 渗透测试、漏洞修复、安全合规 | 5d |
| Bug修复 | 全员测试阶段收集的问题修复 | 8d |
| 灰度发布 | 分批次放量、监控告警 | 3d |
| 文档完善 | API文档更新、用户手册 | 2d |
| 运维部署 | 生产环境部署、监控告警配置 | 3d |
| **小计** | | **26d** |

---

### 7.5 人力资源配置建议

| 角色 | 人数 | 职责 | 周期 |
|------|------|------|------|
| 前端开发 | 2人 | Vue3组件开发、页面交互 | 全程 |
| 后端开发 | 4人 | API开发、数据处理 | 全程 |
| UI设计 | 1人 | 页面设计、组件设计 | 第1-3周 |
| 测试 | 2人 | 功能测试、自动化测试 | 第2-4周 |
| 运维 | 1人 | 部署、监控、运维 | 第3-4周 |

### 7.6 技术架构升级建议

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│    Web (Vue3) / App (Flutter) / 小程序 / Chrome插件          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        网关层                                 │
│    Kong / Nginx (限流/鉴权/路由)                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      应用服务层                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 用户服务  │ │ 选品服务  │ │ 内容服务  │ │ 课程服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 数据分析  │ │ 运营服务  │ │ AI服务   │ │ 消息服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                 │
│  MySQL │ Redis │ MongoDB │ Elasticsearch │ 时序数据库       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        第三方服务                             │
│  OpenAI / Stripe / 阿里云SMS / 腾讯云VOD / 1688 API          │
└─────────────────────────────────────────────────────────────┘
```

---

## 附录

### A. 外部依赖清单

| 服务 | 用途 | 成本预估 |
|------|------|----------|
| OpenAI API | AI选品分析、Listing生成 | $500-2000/月 |
| Stripe | 支付处理 | 2.9%+$0.30/笔 |
| 阿里云SMS | 短信验证 | ¥0.045/条 |
| 腾讯云VOD | 视频存储播放 | ¥0.1/GB存储 |
| SendGrid | 邮件发送 | $20/月起 |
| AWS/GCP | 云服务器 | $1000+/月 |

### B. 数据库ER图概要

```
users ──┬── user_memberships ─── membership_plans
        ├── oauth_accounts
        ├── user_devices
        ├── kyc_records
        └── tracked_products ─── product_daily_snapshots

analysis_tasks ─── analysis_results

dashboard_configs ─── dashboard_widgets

courses ─── course_chapters ─── course_lessons ─── learning_progress

activities ─── activity_prizes ─── activity_participations

notifications ─── notification_settings
```

### C. 里程碑定义

| 里程碑 | 完成时间 | 交付内容 |
|--------|----------|----------|
| M1 核心闭环 | 第2周末 | 注册→选品→Listing生成 完整流程跑通 |
| M2 功能完善 | 第3周末 | 所有P0/P1功能开发完成 |
| M3 Beta发布 | 第4周末 | 内部测试+灰度用户试用 |
| M4 正式上线 | 第5周 | 全量发布+监控告警 |

---

> 📝 **文档维护**：每次迭代后更新此文档，确保功能清单与实际开发进度同步。
